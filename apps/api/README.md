# apps/api

Placeholder for the Ask Rich backend API.

Planned responsibilities:
- `/api/chat` endpoint,
- retrieval and prompt assembly,
- provider-agnostic model + embeddings adapters,
- citation-aware response formatting,
- lightweight evaluation and health endpoints.

Target runtime: Cloudflare Worker API.
